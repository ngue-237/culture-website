{%extends 'baseF.html.twig'%}
{% block title %}blog{% endblock %}
{% block content %}
    <!--::header part start::-->
    <header class="main_menu home_menu">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-12">
                    {{include('includes/frontoffice/navbar.html.twig') }}
                </div>
            </div>
        </div>
        <div class="search_input" id="search_input_box">
            <div class="container ">
                <form class="d-flex justify-content-between search-inner">
                    <input type="text" class="form-control" id="search_input" placeholder="Search Here">
                    <button type="submit" class="btn"></button>
                    <span class="ti-close" id="close_search" title="Close Search"></span>
                </form>
            </div>
        </div>
    </header>
    <!-- Header part end-->

    <!--================Home Banner Area =================-->
    <!-- breadcrumb start-->
    <section class="breadcrumb breadcrumb_bg">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="breadcrumb_iner">
                        <div class="breadcrumb_iner_item">
                            <h2>blog</h2>
                            <p>Home <span>-</span> blog</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- breadcrumb start-->
    <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">




        <tr {% for c in articles %}>

            <td><h1>{{ c.titre }}</h1> <a href="{{ path('blogf',{'id':c.id}) }} " class="btn btn-primary">lire</a> <br>
                <br>
                <img src="{{ asset('uploads/images/' ~ c.photo) }}" alt="" style="width:300px;height:300px;">


            </td>




        </tr {% endfor %}>





    </table>

    <h1  >    {{ knp_pagination_render(articles) }}</h1>

    <!-- ================ contact section start ================= -->

    {{include('includes/frontoffice/footer.html.twig')}}
{% endblock %}