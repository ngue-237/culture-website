{% extends 'baseB.html.twig' %}
{% block content %}
    <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
        <thead>
        <tr align="center">
            <th>Réference</th>
            <th>Designation</th>
            <th>Prix</th>
            <th>Cathegorie</th>
            <th>Quantite</th>
            <th>Image</th>
            <th>Action</th>
            <th>Action</th>
            <th>Action</th>
        </tr>
        </thead>

        <tbody>
        {% for i in data %}
            <tr align="center">
                <td>{{ i.id }}</td>
                <td>{{ i.designation }}</td>
                <td>{{ i.prix }}</td>
                <td>{{ i.cathegorie.id }}</td>
                <td>{{ i.quantite }}</td>
                <td>{{ i.image }}</td>
                <td>
                    <a href="{{ path('suppP',{'id' :i.id}) }}" class="btn btn-danger btn-circle">
                        <i class="fas fa-trash"></i>
                    </a>
                </td>
                <td>
                    <a href="{{ path('modifP',{'id' :i.id}) }}" class="btn btn-info btn-circle">
                        <i class="fas fa-info-circle"></i>
                    </a>
                </td>
                <td>
                    <a href="{{ path('produit',) }}" class="btn btn-info btn-circle">
                        <i class="fas fa-info-circle"></i>
                    </a>
                </td>

            </tr>
        {% endfor %}

        </tbody>
    </table>
{% endblock %}